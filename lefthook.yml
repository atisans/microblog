# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - name: Check (lint and format) Web
      run: npm run format -w web {all_files}

    - name: Check (lint and format) API
      run: npm run format -w api {all_files}
