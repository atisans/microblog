{
  "name": "microblog",
  "description": "",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "web": "npm run dev -w web",
    "api": "npm run dev -w api",
    "format": "npm run format -w web -w api"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.2.2",
    "lefthook": "^1.12.3"
  }
}
